mixin slide(img, text)
  +b.slide
    +e.A.thumbnail(href='#')
      img(src= "img/" + img alt= text title= text)
    +e.modal
      img(src= "img/" + img alt= text title= text)
//- можно еще задать width/height, но т.к. изображения в контейнерах, все равно растянутся